<div class="container">
  <% @articles.each do |article| %>
  <article>
    <h2><%= link_to article.title, article_path(article), class: 'text-decoration-none' style:"color" %>
    </h2>
    <%= article.content[0..100]  %>
  </article>
    <% end %>
</div>
